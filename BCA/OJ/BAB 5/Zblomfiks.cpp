#include <stdio.h>

int main () {
    int t;
    scanf("%d", &t);
    for (int tc = 1; tc <= t; tc++) {
        int n,k;
        scanf("%d %d", &n, &k);
        getchar();

        char plot[n+5];
        scanf("%s", plot);
        getchar();

        
    }
    
    return 0;
}